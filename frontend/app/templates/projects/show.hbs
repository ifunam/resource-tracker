<h3></h3>
<div class="box box-with-radius">
  <table class="table table-condensed">
    <thead>
      <tr>
        <th>{{t "project.agreement_and_name"}}</th>
        <th>{{t "project.sponsor_full_name"}}</th>
        <th>{{t "project.agreement"}}</th>
        <th>{{t "project.authorized_budget"}}</th>
        <th>{{t "project.deposited_budget"}}</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>{{agreement_and_name}}</td>
        <td>{{sponsor_full_name}}</td>
        <td>{{agreement}}</td>
        <td>{{authorized_budget}}</td>
        <td>{{deposited_budget}}</td>
      </tr>
    </tbody>
  </table>
</div>

{{outlet}}

{{#link-to 'projects' class='btn btn-success' }} {{t 'project.others'}} {{/link-to}}

