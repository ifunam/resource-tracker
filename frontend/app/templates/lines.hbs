<h3>{{t 'line.others'}} </h3>
  <table class="table table-striped table-condensed table-hover table-bordered">
    <thead>
      <tr>
        <th>{{t 'line.key' }}</th>
        <th>{{t 'line.name' }}</th>
        <th class="numeric">{{t 'line.deposited_budget' }}</th>
        <th class="numeric">{{t 'line.committed_budget' }}</th>
        <th class="numeric">{{t 'line.spent_budget' }}</th>
        <th class="numeric">{{t 'line.balance' }}</th>
      </tr>
    </thead>
    <tbody>
      {{#each line in lines}}
      <tr>
        {{#if line.has_expenditures}}
        <td>{{link-to line.key 'lines.show' line}}</td>
        {{else}}
        <td>{{line.key}}</td>
        {{/if}}
        <td>{{line.name}}</td>
        <td class="numeric">{{line.deposited_budget}}</td>
        <td class="numeric">{{line.committed_budget}}</td>
        <td class="numeric">{{line.spent_budget}}</td>
        <td class="numeric">{{line.balance}}</td>
      </tr>
      {{/each}}
    </tbody>
    {{#if meta}}
    <tfoot>
      <tr>
        <td class="bold" colspan="2">Total</td>
        <td class="numeric">{{meta.total.deposited_budget}}</td>
        <td class="numeric">{{meta.total.committed_budget}}</td>
        <td class="numeric">{{meta.total.spent_budget}}</td>
        <td class="numeric">{{meta.total.balance}}</td>
      </tr>
    </tfoot>
    {{/if}}
  </table>
